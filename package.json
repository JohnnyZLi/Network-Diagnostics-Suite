{
  "name": "network-diagnostics-suite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-speed-assets.mjs && vite",
    "build": "tsc -b && node scripts/generate-speed-assets.mjs && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "design-system:update": "node scripts/update-design-system.mjs",
    "design-system:sync": "node scripts/check-design-system.mjs --write",
    "design-system:check": "node scripts/check-design-system.mjs",
    "design-system:integration": "node scripts/validate-design-system-integration.mjs",
    "design-system:conformance": "node scripts/design-system-conformance-runner.mjs --contract scripts/design-system-conformance-contract.json",
    "types:worker": "wrangler types",
    "worker:dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "r2:generate": "node scripts/generate-r2-speed-object.mjs",
    "r2:bucket:create": "wrangler r2 bucket create network-diagnostics-speed",
    "r2:upload": "npm run r2:generate && wrangler r2 object put network-diagnostics-speed/network-diagnostics-speed-v1.bin --remote --file .r2-speed/network-diagnostics-speed-v1.bin --content-type application/octet-stream --cache-control \"no-store, no-transform\" --force",
    "r2:cors": "wrangler r2 bucket cors set network-diagnostics-speed --file infra/r2-cors.json",
    "r2:verify": "node scripts/verify-r2-speed.mjs",
    "probe:test": "dotnet test tools/DeepProbe.Tests/NetworkDeepProbe.Tests.csproj -c Release",
    "probe:build": "npm run probe:build:win-x64 && npm run probe:build:mac-arm64 && npm run probe:build:mac-x64 && npm run probe:build:linux-x64 && npm run probe:build:linux-arm64",
    "probe:build:win-x64": "dotnet publish tools/DeepProbe/NetworkDeepProbe.csproj -c Release -r win-x64 --self-contained true --output artifacts/win-x64",
    "probe:build:mac-arm64": "dotnet publish tools/DeepProbe/NetworkDeepProbe.csproj -c Release -r osx-arm64 --self-contained true --output artifacts/osx-arm64",
    "probe:build:mac-x64": "dotnet publish tools/DeepProbe/NetworkDeepProbe.csproj -c Release -r osx-x64 --self-contained true --output artifacts/osx-x64",
    "probe:build:linux-x64": "dotnet publish tools/DeepProbe/NetworkDeepProbe.csproj -c Release -r linux-x64 --self-contained true --output artifacts/linux-x64",
    "probe:build:linux-arm64": "dotnet publish tools/DeepProbe/NetworkDeepProbe.csproj -c Release -r linux-arm64 --self-contained true --output artifacts/linux-arm64"
  },
  "dependencies": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260729.1",
    "@types/node": "26.1.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.3",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  },
  "engines": {
    "node": ">=24"
  }
}
